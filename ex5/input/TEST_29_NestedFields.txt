class Node
{
	int value;
	Node next;
}

void main()
{
	Node head := new Node;
	head.value := 1;
	
	head.next := new Node;
	head.next.value := 2;
	
	head.next.next := new Node;
	head.next.next.value := 3;
	
	PrintInt(head.value);
	PrintInt(head.next.value);
	PrintInt(head.next.next.value);
}
